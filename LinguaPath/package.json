{
  "name": "linguapath-backend",
  "version": "1.0.0",
  "type": "commonjs",
  "scripts": {
    "build": "cd frontend && npm run build",
    "deploy": "cd frontend && npm run build && cd .. && firebase deploy --only hosting"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "mysql2": "^3.22.3"
  }
}
